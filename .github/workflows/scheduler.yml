name: Stock Pipeline Scheduler

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 5 minutes
  workflow_dispatch:       # Manual run button

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run fetch script
        env:
          DATABASE_URL: ${{ secrets.NEON_DB_URL }}
        run: python pipeline/fetch_stocks.py

      - name: Run metrics calculation
        env:
          DATABASE_URL: ${{ secrets.NEON_DB_URL }}
        run: python pipeline/calculate_metrics.py
      
      - name: Generate AI Insights
        env:
          DATABASE_URL: ${{ secrets.NEON_DB_URL }}
          HF_API_TOKEN: ${{ secrets.HF_API_TOKEN }}
        run: python pipeline/generate_ai_insights.py